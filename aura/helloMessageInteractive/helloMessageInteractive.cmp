<aura:component >
    <aura:attribute name = "message" type="string" default="Hello"/>
    
    <p>message of the Day:
        {!v.message}
    </p>
    <div>
    <lightning:button label = "You look nice today." onclick="{!c.handleClick}"/>
    <lightning:button label = "Today is going to be a great Day!." onclick="{!c.handleClick2}"/>
    </div>        
</aura:component>